<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="review">
	
	<!-- 리뷰 등록 -->
	<insert id="regist" parameterType="reviewDto">
		insert into review values(
			review_seq.nextval, #{member_code}, #{order_code},
			#{title}, #{content}, 0, #{score},'작성완료', '답변대기','찾아주셔서 감사합니다 고객님',
			sysdate, sysdate
		)
	</insert>

	<!-- 리뷰 목록 -->
	<select id="list" resultType="shopReviewVO">
		select * from shop_orders_review order by no desc
	</select>
</mapper>